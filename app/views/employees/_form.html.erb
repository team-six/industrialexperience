<%= form_for(@employee) do |f| %>
  <% if @employee.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@employee.errors.count, "error") %> prohibited this employee from being saved:</h2>

      <ul>
      <% @employee.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <ul class="basic-form">
    <li>
      <%= f.label "First Name" %>
      <%= f.text_field :employee_fname, :class => "input" %>
    </li>

    <li>
      <%= f.label "Last Name" %>
      <%= f.text_field :employee_lname, :class => "input" %>
    </li>

    <li><%= f.label "Employment Commencement" %>
      <%= f.date_select :employee_started, :class => "input" %>
    </li>

    <li><%= f.label "Contact Number" %>
      <%= f.text_field :employee_contact_num, :class => "input" %>
    </li>

    <li><%= f.label "Email Address" %>
      <%= f.text_field :employee_email, :class => "input" %>
    </li>

     <li><%= f.label "Religion" %><br>
     <select name="employee[religion_id]">
      <% @religions.each do |religion| %>
        <option value = <%= religion.id %> ><%= religion.religion_name.capitalize %></option>
      <% end %>
     </select>
    </li>
  </ul>

  <%= f.hidden_field :user_id, :value => current_user.id %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
